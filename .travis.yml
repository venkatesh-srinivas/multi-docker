sudo: required
services:
  - docker

before_install:
  - docker build -t venkatesh-srinivas/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run venkatesh-srinivas/react-test npm test -- --coverage

after_success:
  - docker build -t venkatesh-srinivas/multi-client ./client
  - docker build -t venkatesh-srinivas/multi-nginx ./nginx
  - docker build -t venkatesh-srinivas/multi-server ./server
  - docker build -t venkatesh-srinivas/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push venkatesh-srinivas/multi-client
  - docker push venkatesh-srinivas/multi-nginx
  - docker push venkatesh-srinivas/multi-server
  - docker push venkatesh-srinivas/multi-worker

